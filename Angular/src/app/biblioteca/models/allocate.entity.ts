export class Allocate {
    /**
    * @property {id} id id da alocação
    */
    id: string;

    /**
    * @property {usuario} usuario usuário que alocou
    */
    usuario: string;

    /**
    * @property {livro} livro livro alocado
    */
    livro: string;

    /**
    * @property {data_alocacao} data_locacao data de locação
    */
    data_alocacao: string;

    /**
    * @property {data_devolucao} data_devolucao data de devolução
    */
    data_devolucao: string;
}